# Company registry (Job 05 — SEC EDGAR).
#
# Prompt.md section 13: "Maintain a company -> CIK registry." Start with a
# curated list rather than attempting to enumerate every possible filer
# (Prompt.md section 11 makes the same recommendation for company pipeline
# pages: "Start with a curated company registry rather than attempting
# unrestricted web crawling").
#
# CIKs verified live against SEC's own lookup services on 2026-08-11:
# https://www.sec.gov/files/company_tickers.json (currently-listed filers)
# and https://www.sec.gov/cgi-bin/browse-edgar?action=getcompany (retains
# delisted/acquired filers' historical CIKs indefinitely).
#
# A company can have MORE THAN ONE CIK: a name change alone keeps the same
# CIK (SEC records it under `formerNames`, e.g. Seagen/"Seattle Genetics"),
# but a corporate redomicile/reincorporation creates a brand-new SEC filer
# identity with its own CIK and its own filing history. Confirmed live on
# 2026-08-11 (checked every company below via its submissions API's
# `formerNames`) that Zymeworks is the only case in this registry: it
# redomiciled from British Columbia to Delaware in 2022, so its full
# pre-2022 history (including any ADC platform/partnership disclosures from
# that period) lives under a different, predecessor CIK. `ciks` is always a
# list — single-CIK companies just have a one-item list — so every filing's
# query_id can carry which specific filer entity it came from.
#
# company_id is a short stable slug used to build this company's query_id
# in the SEC job (e.g. "seagen" CIK 0001060736 -> query_id
# "SEC_FILINGS_SEAGEN_0001060736"). Do not rename an existing company_id
# once records reference it.
#
# EXTENDED for Job 11 (company pipeline pages, Prompt.md section 11) and
# Job 12 (company press releases, Prompt.md section 12) on 2026-08-14/17:
# official_domain/pipeline_urls/investor_relations_url/press_release_url/
# press_release_template, per the SAME Company entries SEC already uses
# (Prompt.md's own section 11 names the registry file identically to
# SEC's) — see adc_acquisition/company_registry.py for why one shared
# dataclass, not a second competing registry. Live-verified 2026-08-14
# (plain requests + a descriptive User-Agent, same technique already used
# for fda.gov's bot detection): a company acquired/delisted can still have
# NO standalone pipeline page at all (its former assets appear only in the
# acquirer's own pipeline page instead) -- `pipeline_urls: []` for those,
# not a guessed/broken URL. `active` continues to mean "still worth
# tracking historically" (as SEC already uses it) -- it does NOT mean
# "has a pipeline page"; that's simply an empty pipeline_urls list.
#
# `press_release_template` (added 2026-08-17, Job 12): each company's IR
# newsroom is a bespoke site with its own listing-page HTML structure and
# pagination scheme -- no shared API exists across companies (same
# "fundamentally different from database APIs" framing Prompt.md gives
# pipeline pages). Live-verified 2026-08-17 that these fall into a small
# number of REUSED third-party IR-platform templates rather than each
# being genuinely unique:
#   - "workiva_ir_newsroom": <li class="wd_item"> listing items,
#     offset-based `?o=N` pagination (ADC Therapeutics, AbbVie -- byte-
#     identical template between the two, confirmed live).
#   - "q4_ir_media": <article class="media"> listing items with an ISO
#     `datetime` attribute, 1-indexed `?page=N` pagination (Sutro
#     Biopharma).
#   - "pfizer_drupal_newsroom": Pfizer's own Drupal-based newsroom,
#     0-indexed `?page=N` pagination -- a genuinely one-off template, not
#     shared with any other registered company.
# See jobs/company_press_release/parser.py for the per-template parsers
# this field selects between.
#
# `presentations_url`/`presentations_template` (added 2026-08-24,
# BREADTH_PLAN.md Phase 5 Part 7 -- company scientific-presentation
# source): distinct from `press_release_url` -- an IR newsroom covers
# corporate announcements, this covers actual scientific congress
# presentations/posters (AACR/ASCO/ESMO/ASH/etc.), which is often hosted
# on a SEPARATE page or even a separate domain from the press-release
# feed (confirmed live 2026-08-24: ADC Therapeutics' scientific
# presentations live on adctmedical.com, a distinct medical-affairs
# microsite, not adctherapeutics.com or ir.adctherapeutics.com -- see
# jobs/company_scientific_presentations/job.py for why its own-domain
# check is anchored to `presentations_url`'s own host, not
# `official_domain`). Only registered for companies where a real,
# scrapable listing with genuine congress-presentation content was found
# live -- NOT attempted for every company (see that job's report for
# which companies were checked and found to have no such page: AbbVie's
# main domain is behind the same Cloudflare challenge documented above
# for its pipeline page and no separate public microsite was found;
# Pfizer has no distinct presentations archive, only stale 2018-2020
# press-kit assets; Seagen/ImmunoGen/Mersana are acquired/absorbed with
# no standalone page, same as their pipeline/press-release situation).
# Templates:
#   - "adctmedical_congress_listing": a single unpaginated page (~115
#     items, all loaded at once, confirmed live) of `<div class='pub_row
#     ...'>` blocks, each linking DIRECTLY to a PDF poster/slide-deck file
#     (no separate HTML detail page) -- no per-item date beyond the
#     congress year, preserved via the extra `congress` field ("ASH
#     2025") rather than fabricated into a false-precision date.
#   - "sutro_divi_blog": a Divi-theme (Elegant Themes) WordPress blog
#     loop, `<article id="post-N" ...>` items with a clean `entry-title`/
#     `post-meta published` date, standard `/page/N/` pagination --
#     confirmed live that pages past the real end return zero matching
#     items (a static, always-present "page"-type post exists on every
#     page but does not match the entry-title pattern, so it never
#     produces a spurious item). Each item's own URL is an HTML detail
#     page (not a direct PDF) -- materialized as-is, same "don't chase
#     embedded assets one hop deeper" principle as Job 12's press-release
#     detail pages.

companies:
  - company_id: seagen
    canonical_name: Seagen Inc.
    ciks: ["0001060736"]
    aliases: ["Seattle Genetics", "Seattle Genetics Inc"]
    tickers: []
    active: true
    official_domain: seagen.com
    pipeline_urls: []
    investor_relations_url: null
    press_release_url: null
    notes: >
      Acquired by Pfizer, December 2023. Delisted; CIK retained for
      historical filings (name change only, same CIK throughout). No
      standalone pipeline page as of 2026-08-14 -- former Seagen ADC
      assets (PADCEV/enfortumab vedotin, TIVDAK/tisotumab vedotin,
      disitamab vedotin, sigvotatug vedotin) now appear only in Pfizer's
      own oncology pipeline page (see pfizer's pipeline_urls below).

  - company_id: immunogen
    canonical_name: ImmunoGen, Inc.
    ciks: ["0000855654"]
    aliases: []
    tickers: []
    active: true
    official_domain: immunogen.com
    pipeline_urls: []
    investor_relations_url: null
    press_release_url: null
    notes: >
      Acquired by AbbVie, February 2024. Delisted; CIK retained for
      historical filings. No standalone pipeline page as of 2026-08-14 --
      abbvie.com/immunogen.html is an informational redirect page only,
      not a pipeline listing. Former ImmunoGen ADC assets now appear only
      in AbbVie's own pipeline page (see abbvie's pipeline_urls below,
      noting its own live-verified access limitation).

  - company_id: mersana
    canonical_name: Mersana Therapeutics, Inc.
    ciks: ["0001442836"]
    aliases: []
    tickers: []
    active: true
    official_domain: mersana.com
    pipeline_urls: []
    investor_relations_url: null
    press_release_url: null
    notes: >
      Acquired by Day One Biopharmaceuticals, completed 2026-01-06.
      mersana.com/pipeline/ now 302-redirects to dayonebio.com; Mersana's
      lead ADC asset (Emi-Le / emiltatug ledadotin) moved to Day One's own
      pipeline. Day One is NOT yet a registered company in this file --
      tracking its pipeline page would require adding it as a new entry,
      a scope decision left for a future round rather than assumed here.

  - company_id: zymeworks
    canonical_name: Zymeworks Inc.
    ciks: ["0001937653", "0001403752"]
    aliases: []
    tickers: ["ZYME"]
    active: true
    official_domain: zymeworks.com
    pipeline_urls: ["https://www.zymeworks.com/pipeline/"]
    investor_relations_url: https://ir.zymeworks.com/
    press_release_url: https://ir.zymeworks.com/news-releases
    press_release_template: null
    notes: >
      Two CIKs, not a name change: 0001937653 is the current Delaware entity
      (used since the 2022-07 redomicile), which SEC's own submissions API
      shows was itself briefly named "Zymeworks Delaware Inc." before the
      2022-10-13 rename to "Zymeworks Inc." 0001403752 is the predecessor —
      "Zymeworks BC Inc." (British Columbia), named "Zymeworks Inc." until
      the redomicile — and holds the entire pre-2022-10-13 filing history.
      Both must be pulled to get Zymeworks' complete filing set. Pipeline
      page verified live 2026-08-14: plain static HTML (ZW191, ZW251,
      ZW220, ZW209, ZW1528, zanidatamab variants), no JS rendering needed.
      LIVE-VERIFIED ACCESS LIMITATION (2026-08-17, Job 12): the ENTIRE
      ir.zymeworks.com subdomain (not just the news-releases page) is
      unreachable -- confirmed twice with a direct `requests` call and a
      descriptive User-Agent, both a plain GET to ir.zymeworks.com/ and to
      the news-releases page itself hang to a 30s read timeout with zero
      bytes ever received (TLS handshake completes, then nothing) --
      distinct from AbbVie's fast Cloudflare 403 challenge response.
      www.zymeworks.com (the pipeline page's own domain) is unaffected.
      press_release_template left null since the page's actual HTML
      structure has never been observed; every fetch attempt against this
      URL is recorded as a normal, logged `failed` attempt (timeout), not
      silently dropped or worked around, until this becomes reachable
      again or Zymeworks offers an alternative route.

  - company_id: sutro_biopharma
    canonical_name: Sutro Biopharma, Inc.
    ciks: ["0001382101"]
    aliases: []
    tickers: ["STRO"]
    active: true
    official_domain: sutrobio.com
    pipeline_urls: ["https://www.sutrobio.com/pipeline/"]
    investor_relations_url: https://ir.sutrobio.com/
    press_release_url: https://ir.sutrobio.com/news-events/news-releases
    press_release_template: q4_ir_media
    presentations_url: https://www.sutrobio.com/news/presentations/
    presentations_template: sutro_divi_blog
    notes: >
      Pipeline page verified live 2026-08-14: plain static HTML tables
      (STRO-004, STRO-006, STRO-227, STRO-00Y proprietary programs; VAX-31,
      VAX-24, ASP2998 partnered programs), no JS rendering needed.
      Press-release listing verified live 2026-08-17: registry URL
      301-redirects to https://ir.sutrobio.com/press-releases (a Q4 IR
      platform template), 10 releases/page via `?page=N` (1-indexed),
      ~230+ releases going back years. Also has an RSS feed
      (/press-releases/rss) not currently used by this job. Individual
      release URLs stay on ir.sutrobio.com -- no wire-service redirect.
      SCIENTIFIC PRESENTATIONS (verified live 2026-08-24): a SEPARATE
      WordPress category page on the main corporate domain (not
      ir.sutrobio.com), a Divi-theme blog loop mixing real AACR/ASCO/World
      ADC Summit/R&D-Day presentation posts with some plain announcement
      posts under the same "presentations" category -- acquired broadly,
      same "acquire broadly, filter downstream" principle as the press-
      release feed. Standard `/page/N/` pagination, at least 8 full pages
      (80+ items) confirmed live, going back to at least 2025-05; a static
      "page"-type post (id post-3163) appears on every paginated URL
      including far past the real end but does NOT match the entry-title
      pattern, so pages past the real end correctly parse to zero items.
      Each item's own URL is an HTML detail page on sutrobio.com, not a
      direct PDF -- materialized as the acquisition artifact as-is.

  - company_id: adc_therapeutics
    canonical_name: ADC Therapeutics SA
    ciks: ["0001771910"]
    aliases: []
    tickers: ["ADCT"]
    active: true
    official_domain: adctherapeutics.com
    pipeline_urls: ["https://www.adctherapeutics.com/our-pipeline1-1/"]
    investor_relations_url: https://ir.adctherapeutics.com/
    press_release_url: https://ir.adctherapeutics.com/news
    press_release_template: workiva_ir_newsroom
    presentations_url: https://www.adctmedical.com/congresses/
    presentations_template: adctmedical_congress_listing
    notes: >
      Swiss company, files as a foreign private issuer where applicable.
      Pipeline page verified live 2026-08-14: plain static HTML
      (ZYNLONTA/loncastuximab tesirine-lpyl, LOTIS-2/5/7 trials), no JS
      rendering needed. CORRECTED 2026-08-17 (Job 12): the previously
      registered press_release_url (ir.adctherapeutics.com/press-releases)
      now 301-redirects to a dead /404 page (confirmed live, HTTP 200 body
      is the site's own 404 page) -- the current correct URL is
      ir.adctherapeutics.com/news, a Workiva-hosted IR newsroom template
      (`<li class="wd_item">`, offset-based `?o=N` pagination, confirmed
      up to o=220, ~224 releases total, genuinely returns 0 items past the
      real end). Individual release URLs stay on ir.adctherapeutics.com
      (date-prefixed slugs, e.g. /2026-08-13-ADC-Therapeutics-...) -- no
      wire-service redirect. SCIENTIFIC PRESENTATIONS (verified live
      2026-08-24): NOT on adctherapeutics.com or ir.adctherapeutics.com --
      a separate "Medical Information Hub" microsite at adctmedical.com
      (also ADC Therapeutics' own official medical-affairs site, confirmed
      via its page title/branding) has a single unpaginated congress-
      presentations listing, 115 distinct entries confirmed live
      (AACR/ASCO/ASH/EHA/ICML/CTOS/EORTC-NCI-AACR/PPLC, 2015-2025), each
      linking DIRECTLY to a PDF poster/slide-deck file hosted on the same
      domain -- no separate HTML detail page, no pagination needed (all
      entries load on the one page). No per-item date finer than the
      congress year.

  - company_id: abbvie
    canonical_name: AbbVie Inc.
    ciks: ["0001551152"]
    aliases: []
    tickers: ["ABBV"]
    active: true
    official_domain: abbvie.com
    pipeline_urls: ["https://www.abbvie.com/science/pipeline.html"]
    investor_relations_url: null
    press_release_url: https://news.abbvie.com/
    press_release_template: workiva_ir_newsroom
    notes: >
      Large diversified filer post-ImmunoGen acquisition; expect
      substantial non-ADC-relevant filing/pipeline volume — acquisition
      layer preserves everything regardless, relevance filtering is
      downstream. LIVE-VERIFIED ACCESS LIMITATION (2026-08-14): the
      pipeline URL is behind a Cloudflare JS challenge ("Just a
      moment..." interstitial, HTTP 403) -- confirmed this is NOT the
      same simple User-Agent-sniffing block fda.gov had (a descriptive
      User-Agent does not help here); it requires executing JavaScript to
      pass, which this repo does not attempt (would cross into the
      CAPTCHA/bot-challenge-bypassing territory Prompt.md explicitly
      prohibits). The pipeline job will record every attempt against this
      URL as a normal, logged `failed` attempt (never silently dropped)
      until AbbVie's own official channels offer an alternative
      machine-readable route. PRESS RELEASES (Job 12, verified live
      2026-08-17): news.abbvie.com is a SEPARATE subdomain from
      abbvie.com and is NOT behind the same Cloudflare challenge --
      confirmed a plain HTTP 200 with a descriptive User-Agent. It's a
      Workiva-hosted IR newsroom, byte-identical template to ADC
      Therapeutics' (`?o=N` offset pagination, `<li class="wd_item">`
      listing items, date-prefixed release slugs). This feed covers
      AbbVie's ENTIRE newsroom (all therapeutic areas, not ADC-specific)
      -- same "acquire broadly, filter downstream" caveat as the pipeline
      page above; likely hundreds of releases across AbbVie's full
      history, so an initial full-history discovery sweep here is
      expected to walk many pages (see jobs/company_press_release/job.py's
      MAX_PAGES safety cap and its incremental early-stop logic for
      subsequent runs).

  - company_id: pfizer
    canonical_name: Pfizer Inc.
    ciks: ["0000078003"]
    aliases: []
    tickers: ["PFE"]
    active: true
    official_domain: pfizer.com
    pipeline_urls: ["https://www.pfizer.com/science/oncology-cancer/pipeline"]
    investor_relations_url: null
    press_release_url: https://www.pfizer.com/newsroom/press-releases
    press_release_template: pfizer_drupal_newsroom
    notes: >
      Large diversified filer post-Seagen acquisition; same "acquire
      broadly, filter downstream" caveat as AbbVie above. Pipeline page
      verified live 2026-08-14: plain static HTML, accessible with a
      descriptive User-Agent (unlike AbbVie's Cloudflare challenge) --
      confirmed real ADC program names present in the raw response
      (PADCEV/enfortumab vedotin, TIVDAK/tisotumab vedotin, disitamab
      vedotin, sigvotatug vedotin), no JS rendering needed. The oncology-
      specific pipeline URL is used rather than pfizer.com/pipeline (the
      full cross-therapeutic-area product pipeline) to keep this
      registered URL reasonably scoped, though Pfizer's overall pipeline
      is still far broader than ADCs alone -- same downstream-filtering
      caveat applies. CORRECTED 2026-08-17 (Job 12): the previously
      registered press_release_url (pfizer.com/news/press-release) is a
      hard 404 (confirmed live) -- the current correct URL is
      pfizer.com/newsroom/press-releases, a one-off Drupal-based template
      (`?page=N` 0-indexed pagination, `<h5><a href="/news/press-release/
      press-release-detail/{slug}">` listing items, relative URLs).
      Confirmed real ADC-relevant releases present across at least 8
      pages of history (PADCEV, disitamab vedotin, Arvinas/vepdegestrant
      collaboration). Requesting far past the real end does not go empty
      (clamps to repeating a previously-seen page instead of ADC
      Therapeutics'/AbbVie's Workiva template, which genuinely empties
      out) -- handled by the job's own already-discovered-URL stop
      condition rather than relying on an empty-page signal.
