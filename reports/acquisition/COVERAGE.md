# Acquisition Coverage

Status matrix across all planned sources (Prompt.md sections 5–19, execution order in section 29).
Update this table whenever a source's `implementation_status` changes in `configs/sources.yaml`.

| Source | Type | Implemented | API/Bulk | Raw Download | Incremental | Records | Status |
|---|---|---|---|---|---|---:|---|
| PubMed | Literature | Yes | Yes (E-utilities) | Yes (per-PMID XML) | Yes (`--resume` + date range) | 20 (test run) | Implemented, phase-1 reviewed |
| Europe PMC | Literature | Yes | Yes (REST + fullTextXML) | Yes (per-record JSON + OA full text) | Yes (`--resume` + date range) | 20 (test run) | Implemented, phase-2 |
| ClinicalTrials.gov | Clinical | Yes | Yes (API v2) | Yes (per-NCT-ID JSON) | Yes (`--resume` + date range) | 20 (test run) | Implemented, phase-3 |
| Crossref | Literature | Yes | Yes (`/works/{doi}`) | Yes (per-DOI JSON) | N/A — DOI-centric reconciliation, not date range | 24 (test run) | Implemented, phase-4 |
| SEC | Regulatory | Yes | Yes (submissions API) | Yes (per-filing + exhibits) | Yes (`--resume` + `filing_date` range, client-side) | 12 (test run) | Implemented, phase-5 |
| FDA | Regulatory | Yes | Yes (label full-text search + drugsfda API) | Yes (per-application raw record + per-submission JSON + documents) | Yes (`--resume` + `submission_status_date` range, client-side) | 20 (test run) | Implemented (approved products only), phase-6 |
| EMA | Regulatory | Yes | Yes (bulk medicines + EPAR-documents JSON feeds) | Yes (per-medicine raw record + documents + raw bulk snapshots) | Yes (`--resume` + `last_updated_date` range, client-side) | 16 (test run) | Implemented (PSUSA/DHPC safety feeds not covered), phase-7 |
| WIPO | Patent | Yes | Yes (EPO OPS, not WIPO PATENTSCOPE — see notes) | Yes (per-publication biblio XML) | Full undated discovery sweep every run; `--since`/`--until` server-side CQL; already-successful publications skipped by default (no OPS request) as an efficiency default, with `--refresh` to periodically re-verify for OPS-side corrections | 20 (test run) | Implemented, phase-8. WIPO PATENTSCOPE itself has no public API and its Terms of Use forbid automated access — acquired via EPO OPS instead, filtered to WO-prefixed publications |
| USPTO | Patent | Yes | Yes (USPTO Open Data Portal, Patent File Wrapper API) | Yes (per-application raw JSON + SPEC documents) | Full undated discovery sweep every run; `--since`/`--until` server-side bracket-range filter; every application refetched and hash-compared every run (mutable content); SPEC documents skipped once fetched (identity-based, not hash-based) | 10 (test run) | Implemented, phase-9. PatentsView shut down 2026-03-20 — acquired via USPTO's own ODP instead |
| EPO | Patent | Yes | Yes (EPO OPS, same account as WIPO) | Yes (per-publication biblio XML) | Full undated discovery sweep every run; `--since`/`--until` server-side CQL; already-successful publications skipped by default (no OPS request) as an efficiency default, with `--refresh` to periodically re-verify | 30 (test run) | Implemented, phase-10. Architecturally independent from WIPO (own query_id/provenance), filtered to EP-prefixed publications. Live-verified EP-specific OPS limitation: title-field phrase search is broken for `pn=EP` at Range>1 — phrase queries use abstract-only (disclosed gap, see epo.md) |
| Company pipelines | Corporate | Yes | No (no official API; curated `configs/company_registry.yaml`) | Yes (per-page HTML/PDF snapshot) | N/A — always refetched/hash-compared every run, no date field to filter by | 5 (test run) | Implemented, phase-11. Seagen/ImmunoGen/Mersana have no standalone pipeline page (absorbed into acquirers); AbbVie's page is behind a Cloudflare JS challenge (documented failure, not bypassed) |
| Company PR | Corporate | Yes | No (no official API; per-company IR newsroom, 3 reused templates) | Yes (per-release HTML/PDF snapshot) | Discovery early-stops once a page contributes zero not-yet-resolved items (efficiency, not a date filter); `--since`/`--until` filter MATERIALIZATION client-side by listing-provided release date; already-successful releases skipped by default, `--refresh` re-verifies AND disables the discovery early-stop | 8 (test run, across 2 companies) | Implemented, phase-12. Separate job from Job 11 (discrete dated announcements, not a current-state snapshot). Zymeworks' ir.zymeworks.com subdomain is currently entirely unreachable (documented failure, not bypassed) |
| Patent bioactivity corpus | Derived | Yes | Yes (EPO OPS description/claims endpoints, same account as Jobs 08/10) | Yes (per-publication description + claims XML, 2 independent artifacts each) | No discovery ledger (candidates read from Job 08's wipo.parquet AND Job 10's epo.parquet); `--since`/`--until` filter candidate selection client-side; already-successful artifacts skipped by default, `--refresh` re-verifies; `not_available` (404) retried every ordinary run, not terminal | 110 (test run: 38 WIPO + 72 EPO artifacts materialized) | Implemented, phase-13. Second-pass job, does NOT search the patent universe again. Round-1 fix corrected an initial "OPS full text is EP-only" overreach (n=1) — WIPO candidates are attempted exactly like EPO's; live empirical result: WIPO 38/40 artifacts available (95%), EPO 72/90 (80%), reported separately per authority. USPTO (Job 09) not duplicated since its SPEC documents already have full text |
| Publication bioactivity corpus | Derived | Yes | Yes (Unpaywall API for doi->OA-location lookup; Europe PMC's own fullTextXML endpoint for pmcid-only records; NCBI's PMC ID Converter for pmid-only records; then a direct fetch of whatever publisher/repository host serves it) | Yes (per-record HTML/PDF/XML snapshot) | No discovery ledger (candidates read from Job 01's pubmed.parquet, Job 02's europe_pmc.parquet, and Job 04's crossref.parquet); `--since`/`--until` filter candidate selection client-side; already-successful records skipped by default, `--refresh` re-verifies; `not_available` retried every ordinary run, not terminal | 3 (test run, of 38 unique candidate records) | Implemented, phase-14. Second-pass job, does NOT search the literature universe again. Round-1 fix: candidates now include PMID/PMCID-only records (not doi-only, which silently dropped 8/20 PubMed + 6/20 Europe PMC real records) via doi>pmcid>pmid exact-identifier resolution; within one OA location, a blocked PDF link now falls back to that location's landing page before trying a different location; `not_available`'s http_status is now truthful (never fabricated). Excludes records Job 02 already has resolved full text for (by pmcid, avoiding duplication). Live result: 24 doi-addressable + 0 pmcid-addressable + 14 pmid-only (all 14 confirmed unresolvable by NCBI for these older papers) = 38 candidates; 3 success, 34 not_available, 1 failed (Wiley 403) |
| Known-ADC asset expansion | Derived | No | — | — | — | 0 | Not started |

Per-source reports live at `reports/acquisition/<source>.md`.
